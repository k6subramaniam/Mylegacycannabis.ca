Fix analytics trackCheckoutComplete and restore helmet dependency

Fixes applied for PR 165 review feedback:
- Replaced `pushEvent` with `navigator.sendBeacon` in `trackCheckoutComplete` within `useBehaviorTracker.ts` to ensure analytics flush synchronously during page unloads.
- Verified that `helmet` is present in `package.json` dependencies.
- Maintained `app.use(helmet(...))` in `server/_core/index.ts` to preserve security configurations.
- Verified product JSON-LD logic using `getProductBySlug` and `getProductReviewStats` is present in `server/_core/seo-middleware.ts`.
